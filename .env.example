# Copy to .env and fill values for local development
# Path to the trained model file used by the Flask backend
GLUCOGUARD_MODEL=diabetesModel.pkl
# Port the Flask app should listen on
GLUCOGUARD_PORT=5000
# Optional: other secrets (do NOT commit real secrets)
# FIREBASE_API_KEY=
